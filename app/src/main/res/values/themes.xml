<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kioki" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Kioki" parent="Base.Theme.Kioki" />

    <style name="TransparentActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>

    <style name="Theme.App.Small" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="mainTextSize">@dimen/main_text_size_0</item>
    </style>

    <style name="Theme.App.Medium" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="mainTextSize">@dimen/main_text_size_1</item>
    </style>

    <style name="Theme.App.Large" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="mainTextSize">@dimen/main_text_size_2</item>
    </style>



</resources>